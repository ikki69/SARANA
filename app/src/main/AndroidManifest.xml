<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_aset"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_aset"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsetPeminjaman">

        <!-- Splash Screen Activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Main/Dashboard Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Daftar Aset Activity -->
        <activity
            android:name=".DaftarAsetActivity"
            android:exported="false" />

        <!-- Form Peminjaman Activity -->
        <activity
            android:name=".FormPeminjamanActivity"
            android:exported="false" />

        <!-- Riwayat Peminjaman Activity -->
        <activity
            android:name=".RiwayatActivity"
            android:exported="false" />

        <!-- Detail Peminjaman Activity -->
        <activity
            android:name=".DetailPeminjamanActivity"
            android:exported="false" />

    </application>

</manifest>
